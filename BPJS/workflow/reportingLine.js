var organigramID = 0;
var totalLayer = 2;

// _log({ getPosition: getPosition });

var listPositionLayers = [];

for (var i = 1; i <= totalLayer; i++) {
  console.log(totalLayer);
  
  // var filter = {
  //   _filter_version: 2,
  //   where_is_and: {
  //     id_posisi: req.position.id,
  //   },
  // };

  // var sort = { created_at: -1 };
  // var getPosition = _findRecords("nd5MB2OHg", 1, 1, sort, filter, "and") || [];
}
